server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #Nacos服务注册中心地址
      config:
        server-addr: localhost:8848 #Nacos作为配置中心地址
        file-extension: yaml #制定yaml格式的配置
#        group: TAIZHOU_GROUP
#        group: HANGZHOU_GROUP
        namespace: de0af515-f03b-4eaf-ac92-1a65aee99f13
